# {{ pcsm.full_name }} 0.9.0 ({{ date.v0_9_0 }})

[Get started with {{pcsm.short}}](../installation.md){.md-button}

## Release highlights

### Cross-version support

PCSM now supports cross-version replication from MongoDB 6.0 and MongoDB 7.0 source clusters to MongoDB 8.0 target clusters. This enables staged upgrade and migration workflows while maintaining continuous replication between supported MongoDB versions.

At startup, PCSM validates the source and target MongoDB versions against the supported compatibility matrix. If the source cluster is running a **higher major version** than the target cluster, PCSM **prevents replication** from starting and exits with an error.

**Compatibility matrix**

| **Source version** | **Target version** | **Supported**| **Notes** |
|----------------|----------------|-----------|-------|
| 6.0.x | 6.0.x | **Yes** | — |
| 6.0.x | 7.0.x | **Yes** | — |
| 6.0.x | 8.0.x | **Yes** | — |
| 7.0.x | 7.0.x | **Yes** | — |
| 7.0.x | 8.0.x | **Yes** | — |
| 8.0.x | 7.0.x | **No** | Downgrade replication is not supported. |
| 7.0.x | 6.0.x | **No** | Downgrade replication is not supported. |

For more information, see the [documentation](https://docs.percona.com/percona-clustersync-for-mongodb/version-compatibility.html).

### Software Bill of Materials (SBOM)

Percona ClusterSync for MongoDB (PCSM) now provides Software Bills of Materials (SBOMs) for every release. SBOMs improve software supply chain transparency by documenting the components and dependencies included in a build. They are generated automatically as part of the release pipeline in the industry standard `CycloneDX 1.6` format.

SBOMs are distributed through the following channels:

- GitHub: The latest SBOM will be placed in the root of the [percona/percona-clustersync-mongodb](https://github.com/percona/percona-clustersync-mongodb) main branch.

- Binaries/Tarballs: An SBOM file will be included in the root directory of the release archive.

- Packages: For `.deb` and `.rpm` packages, the SBOM file is installed in a standard documentation directory such as `/usr/share/doc/`.

- Docker: The SBOM will be attached directly to the Docker image as metadata.

- Percona Trust Center: We will investigate automating the upload of SBOMs to the Trust Center to maintain a centralized, up-to-date repository.

## Changelog

### New features

* [PCSM-282](https://perconadev.atlassian.net/browse/PCSM-282): Added Software Bills of Materials (SBOMs) for PCSM releases. SBOMs are generated in the `CycloneDX 1.6` format and distributed with release artifacts.

* [PCSM-286](https://perconadev.atlassian.net/browse/PCSM-286): Added support for cross-version replication from MongoDB 7.0 to MongoDB 8.0.

* [PCSM-289](https://perconadev.atlassian.net/browse/PCSM-289): Added support for cross-version replication from MongoDB 6.0 to MongoDB 8.0.

### Improvements

* [PCSM-176](https://perconadev.atlassian.net/browse/PCSM-176): Added detailed finalization status reporting through the `/status` endpoint. You can now monitor the state of the PCSM finalize operation in real time using the `/status` endpoint. While finalization is underway, the `/status` reports that finalization is running. On completion, it returns a structured summary of the finalization results, including details about any **unsuccessful index builds**. This allows you to identify and diagnose index-related issues without the need to manually inspect logs.


### Bugs

* [PCSM-314](https://perconadev.atlassian.net/browse/PCSM-314): Fixed an issue where PCSM could crash during replication when processing an event against an item that did not exist in the target array, causing the replication to halt unexpectedly.

* [PCSM-323](https://perconadev.atlassian.net/browse/PCSM-323): Clone operations previously relied on automatically determined segment sizes. The `--clone-segment-size` option is now available, allowing you to specify a preferred segment size (for example, 500MB or 1GiB) when necessary, thereby improving flexibility and control over clone operations.




